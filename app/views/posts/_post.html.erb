<li>
  <span class="taile"><%=link_to post.title, post %></span>
  <span class="content"><%= post.content %></span>
  <span class="timestamp">
    <%= time_ago_in_words(post.created_at) %>
  </span>
</li>
